Object.entries(); // get the keyvalue pair of the object
variable.toFixed() // When the calculation of floating number occur use toFixed to solve the decimal issue 
//covert number
parseFloat 
parseInt // if item.quantity is "3.14", parseInt(item.quantity) will return 3.
+variable
number(variable)


